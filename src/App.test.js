import { render, screen } from "@testing-library/react";
import App from "./App";

test("should load default theme when detect no local theme", () => {
});

test("should load local theme when detect one", () => {
});

test("should save current theme in local when click on save button", () => {
});

test("should update theme when user update some theme property", () => {
});

test("should render multiple accordions when receive multiple levels design", () => {
});

test("should render multiple panels when receive multiple property level design", () => {
});