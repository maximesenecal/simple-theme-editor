import { render, screen } from "@testing-library/react";
import DesignProperty from "./DesignProperty";

test("should not render edit panel", () => {
});

test("should render edit panel when user click on header panel", () => {
});

test("should close edit panel when user has closed it", () => {
});

test("should replace reference in value when detect one or more", () => {
});
