import { render, screen } from "@testing-library/react";
import EditPanel from "./EditPanel";

test("should close edit panel when click on close button", () => {
});

test("should change type value when select type", () => {
});

test("should update value when change input value", () => {
});

test("should not update current value when change input value", () => {
});

test("should update theme and close panel when click on update button", () => {
});

